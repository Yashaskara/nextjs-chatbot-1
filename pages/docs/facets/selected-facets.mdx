import { ButtonFacet, SelectedFacets, Products } from "@unbxd-ui/react-search-components";
import { Cards, Card, Callout } from "nextra/components";

import CSRWrapper from "../../../components/CSRWrapper";
import Pills from "../../../components/Pills";
import HookIcon from "../../../components/icons/HookIcon";
import CheckboxIcon from "../../../components/icons/CheckboxIcon";
import RadioButtonIcon from "../../../components/icons/RadioButtonIcon";
import CategoryTreeIcon from "../../../components/icons/CategoryTreeIcon";
import RangeSliderIcon from "../../../components/icons/RangeSliderIcon";
import ButtonIcon from "../../../components/icons/ButtonIcon";

# Selected Facets

## Overview

The **SelectedFacets** component displays all the selected filters or facets in a clear and organized manner, providing users with an easy way to review their current filtering criteria. 
This component is particularly useful for enhancing the user experience by offering intuitive controls to remove or deselect individual facets or to clear all selections at once. 
By integrating this component, users can effortlessly manage their filters, ensuring they maintain control over the search and discovery process.


## Usage

```js copy 
// importing component
import { SelectedFacets } from "@unbxd-ui/react-search-components";

// import the style for the component
import "@unbxd-ui/react-search-components/styles/selectedFacets.css";
```

If the styles import does not work, use the following code to import it:

```js copy
require.resolve("@unbxd-ui/react-search-components/styles/selectedFacets.css")
```

<Callout type="warning">
	<div>**Note:**</div>
	<div>
		The **SelectedFacets** component must be used within the wrapper (in CSR, SSR and Custom Hooks approaches) to ensure that the component and the search functionality work properly.
	</div>
</Callout>


## Live Demo

<div
	className="code-example"
	style={{
		display: "flex",
		flexDirection: "column",
		alignItems: "center",
		marginTop: "20px",
		border: "1px solid #ededed",
		borderRadius: "10px",
		overflow: "hidden",
	}}>
	<div style={{ width: "100%", padding: "20px 15px" }}>
		<CSRWrapper apiKey="fb853e3332f2645fac9d71dc63e09ec1" siteKey="demo-unbxd700181503576558"
			defaultValues={{
				pageSize: 8
			}}
			webUrlConfig={{
				query: {
					addToUrl: false,
				},
				sort: {
					addToUrl: false,
				},
				view: {
					addToUrl: false,
				},
				pageSize: {
					addToUrl: false,
				},
				currentPage: {
					addToUrl: false,
				},
				facets: {
					addToUrl: false,
				},
				pagination: {
					addToUrl: false
				}}
			} 
		>
			<div>
                <SelectedFacets />
                <div style={{display: "flex", gap: "20px", margin: "15px 0"}}>
                    <ButtonFacet
                        name="gender_uFilter"
                        renderAs="dropdown"
                        applyAll={true}
                        searchable={true}
                        multiselect={true}
                        clearBtn={true}
                        selectAll={true}
                        showSelectedFacets={true}
                    />
                    <ButtonFacet
                        name="color_uFilter"
                        renderAs="dropdown"
                        applyAll={true}
                        searchable={true}
                        multiselect={true}
                        clearBtn={true}
                        selectAll={true}
                        showSelectedFacets={true}
                    />
                </div>
				<Products />
            </div>
		</CSRWrapper>
	</div>
	<div style={{ width: "100%", borderTop: "1px solid #ededed" }}>
		```js copy {7}
		// code for the above example
		import { UnbxdSearchWrapper } from "@unbxd-ui/react-search-hooks";
		import { ButtonFacet, Products, SelectedFacets } from "@unbxd-ui/react-search-components";

		<UnbxdSearchWrapper>
			...
			<SelectedFacets />
            <ButtonFacet
                name="gender_uFilter"
                renderAs="dropdown"
                applyAll={true}
                searchable={true}
                multiselect={true}
                clearBtn={true}
                selectAll={true}
                showSelectedFacets={true}
            />
            <ButtonFacet
                name="color_uFilter"
                renderAs="dropdown"
                applyAll={true}
                searchable={true}
                multiselect={true}
                clearBtn={true}
                selectAll={true}
                showSelectedFacets={true}
            />
			<Products />
			...
		</UnbxdSearchWrapper>
		```
	</div>
</div>

## Props

<div style={{ position: "relative" }}>
	### showClose
	<Pills pills={[{ type: "type", label: "boolean" }]} left={"145px"} />
</div>

- Determines if the close component must be shown for each selected facet item element.
- Default Value: `true`.

<div style={{ position: "relative" }}>
	### CloseIconComponent
	<Pills pills={[{ type: "type", label: "functional component" }]} left={"265px"} />
</div>

- A component that will be used to render the close icon within each selected facet item element.
- Default Value:

```js copy
const CloseIconComponent = () => {
	return <>&#x2715;</>;
};
```

<div style={{ position: "relative" }}>
	### showDisplayName
	<Pills pills={[{ type: "type", label: "boolean" }]} left={"230px"} />
</div>

- Determines if the facet name must be shown for each selected facet item.
- Default Value: `true`


<div style={{ position: "relative" }}>
	### showClearBtn
	<Pills pills={[{ type: "type", label: "boolean" }]} left={"180px"} />
</div>

- Determines if the clear all facets button must be shown or not.
- Default Value: `true`.


<div style={{ position: "relative" }}>
	### clearBtnLabel
	<Pills pills={[{ type: "type", label: "string" }]} left={"180px"} />
</div>

- The text that will be used for the clear all facets button.
- Default Value: `"Clear all"`.


<div style={{ position: "relative" }}>
	### styles
	<Pills
		pills={[
			{ type: "optional", label: "optional" },
			{ type: "type", label: "object" },
		]}
		left={"95px"}
	/>
</div>

- This prop will be used to style the component. 
- The value must use a defined structure (the structure can be seen in the following example).
- The keys in the structure must be the same while the values can changed as per the user's choice.

```css copy
styles={{
	root: "selected-facets", // used for the wrapper element
	item: "selected-item", // used for each selected facet item element
	label: "selected-label", // used for the label/name of each selected facet item
	closeBtn: "selected-closeBtn" // used for the close button icon
}}
```

## Related Hook

<div style={{ marginTop: "1.5rem", fontSize: "20px", fontWeight: "600" }}>useFacets</div>

To use the Unbxd ReactJS SDK without the pre-built `SelectedFacets` component, add the functionality to your custom component using the useFacets hook. Refer to the documentation for more details.

<Cards>
	<Card icon={<HookIcon />} title="useFacets" href="/docs/useFacets" />
</Cards>

## Related Components

<Cards>
	<Card icon={<ButtonIcon />} title="Button Facet" href="/docs/facets/buttons" />
	<Card icon={<CheckboxIcon />} title="Checkbox Facet" href="/docs/facets/checkbox" />
	<Card icon={<RadioButtonIcon />} title="Radio Button Facet" href="/docs/facets/radio" />
	<Card icon={<CategoryTreeIcon />} title="Multilevel Facet" href="/docs/facets/multilevel" />
	<Card icon={<RangeSliderIcon />} title="Range Facet" href="/docs/facets/range" />
</Cards>